This folder contains all the programs required to run the application.
The file gui.py acts as the Master program.

The only requirement of this application is that all appropriate libraries are installed on the user's machine. No additional software is required.

To launch the application, run the following terminal command from within the directory that contains these files:
python gui.py
